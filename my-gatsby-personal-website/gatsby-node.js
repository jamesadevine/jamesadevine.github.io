const path = require('path');

exports.createPages = async ({ actions }) => {
  const { createPage } = actions;
  // Example for blog post template
  // createPage({
  //   path: '/blog/example',
  //   component: path.resolve('src/templates/blog-post.tsx'),
  //   context: {},
  // });
};
